/** Número de raciones diarias cuando ni el ajuste manual del estanque ni
 * ninguna fila de FeedingRecommendation configuran una (ver
 * resolvePondRationDisplay en pondRation.ts) — nunca 0 (no se podría
 * repartir la ración). */
export const DEFAULT_FEEDINGS_PER_DAY = 2;
